{
  "name": "amux",
  "version": "0.1.0",
  "description": "Terminal multiplexer with first-class git worktree management — tmux + workmux in one binary",
  "main": "dist/server.js",
  "bin": {
    "amux": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/cli.js start",
    "dev": "tsc --watch",
    "install:bin": "node scripts/install-bin.mjs",
    "uninstall:bin": "node scripts/uninstall-bin.mjs",
    "smoke": "npm run build && node scripts/smoke-test.mjs",
    "test": "npm run build"
  },
  "keywords": [
    "terminal",
    "multiplexer",
    "tmux",
    "worktree",
    "workmux",
    "git",
    "pty",
    "cli"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/dnakov/amux"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@types/node": "^25.5.0",
    "@xterm/addon-serialize": "^0.14.0",
    "@xterm/headless": "^6.0.0",
    "node-pty": "^1.1.0",
    "simple-git": "^3.36.0",
    "typescript": "^5.9.3",
    "ws": "^8.19.0",
    "yaml": "^2.8.3"
  },
  "devDependencies": {
    "@types/ws": "^8.18.1"
  }
}
